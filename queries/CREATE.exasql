CREATE TABLE IF NOT EXISTS FORM_APP.FORM_ANSWER (
  answer_id INT PRIMARY KEY,
  session_id INT NOT NULL,
  field_id INT NOT NULL,
  answer_value VARCHAR(2000),
  answered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);